% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vast_model.R
\name{vast_model}
\alias{vast_model}
\title{Create an empty VAST model}
\usage{
vast_model(
  title = "",
  rankdir = "TB",
  naming_mode = c("integrated", "separated", "fimm"),
  fontsize = 12,
  fontsize_nodes = 12,
  fontsize_names = 10,
  fontsize_edges = 10,
  analyst = "",
  bgcolor = "white",
  nodesep = 0.6,
  ranksep = 0.8
)
}
\arguments{
\item{title}{Character. Optional title displayed above the diagram.}

\item{rankdir}{Character. Layout direction: \code{"TB"} (top-to-bottom),
\code{"LR"} (left-to-right), \code{"BT"}, or \code{"RL"}.}

\item{naming_mode}{Character. How concept-name relationships are displayed:
\describe{
\item{\code{"integrated"}}{(Default) Concepts and names coexist in the
diagram, connected by naming (n) arrows.}
\item{\code{"separated"}}{The main diagram shows concepts with abstract
labels only. Naming relationships are displayed in a separate
legend area beside the main diagram.}
\item{\code{"fimm"}}{Finger-is-Moon-Mode. Concept nodes display the
label of their (first) connected name node. Name nodes and
naming edges are hidden from the diagram.}
}}

\item{fontsize}{Numeric. Default global font size (used for graph title).}

\item{fontsize_nodes}{Numeric. Font size for all non-name nodes
(concepts, IS/OUGHT, perspectives, diamonds, data).}

\item{fontsize_names}{Numeric. Font size for name nodes. Useful for
shrinking long natural-language labels independently.}

\item{fontsize_edges}{Numeric. Font size for all edge labels.}

\item{analyst}{Character. Name(s) of the analyst(s) who created
the display (e.g., \code{"Daniel"}, \code{"Daniel, Marcos"}). Displayed
in the top-left corner of the diagram.}

\item{bgcolor}{Character. Background color of the graph.}

\item{nodesep}{Numeric. Minimum horizontal space between nodes (inches).}

\item{ranksep}{Numeric. Minimum vertical space between ranks (inches).}
}
\value{
A list of class \code{vast_model}.
}
\description{
A VAST model is the top-level container holding all nodes, edges,
and groups. Build it up with \code{\link[=add_nodes]{add_nodes()}}, \code{\link[=add_edges]{add_edges()}}, and
\code{\link[=add_groups]{add_groups()}}, then render with \code{\link[=vast_render]{vast_render()}} or convert to
DOT with \code{\link[=vast_to_dot]{vast_to_dot()}}.
}
\details{
All builder functions support R's native pipe (\verb{|>}).
}
\examples{
m <- vast_model(title = "My Argument", naming_mode = "fimm")
m <- vast_model(fontsize_names = 9, fontsize_edges = 8)
m <- vast_model(analyst = "Daniel, Marcos")

}
