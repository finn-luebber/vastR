% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edges.R
\name{vast_relation}
\alias{vast_relation}
\title{Create a VAST relationship (edge)}
\usage{
vast_relation(
  from,
  to,
  type = "c",
  strength = NULL,
  color = "gray30",
  style = "solid",
  penwidth = 1.5,
  fontsize = 10,
  index = NULL,
  lhead = NULL,
  ltail = NULL,
  bidirectional = FALSE
)
}
\arguments{
\item{from}{Character. ID of the source node.}

\item{to}{Character. ID of the target node.}

\item{type}{Character. Relationship type letter. Standard types are
\code{"n"} (naming), \code{"i"} (implication), \code{"c"} (causation),
\code{"t"} (transformation), \code{"p"} (prediction), \code{"r"} (reasoning),
\code{"u"} (unknown). Use \code{""} for a plain structural link with no label.
Custom single letters or abbreviations are also accepted.}

\item{strength}{Numeric, Character, or \code{NULL}. Relationship strength.
Can be a number (e.g., \code{0.5}, \code{-0.3}), a verbal label (\code{"weak"},
\code{"strong"}), a special marker (\code{"?"}, \code{"<> 0"}, \code{"0"}), or \code{NULL}
(unspecified; default positive).}

\item{color}{Character. Edge color (any valid Graphviz color).}

\item{style}{Character. Graphviz edge style: \code{"solid"}, \code{"dashed"},
\code{"dotted"}, \code{"bold"}.}

\item{penwidth}{Numeric. Edge line width.}

\item{fontsize}{Numeric. Font size for the edge label.}

\item{index}{Character or \code{NULL}. Optional index for the coefficient
(e.g., \code{"1"} renders as \code{"c1"} for causation).}

\item{lhead}{Character or \code{NULL}. ID of a group (higher-order concept)
that the arrow should visually point \strong{to}. The arrow will terminate
at the border of that group's bounding box instead of at the target
node. The \code{to} node must be inside that group.}

\item{ltail}{Character or \code{NULL}. ID of a group that the arrow should
visually originate \strong{from}. The arrow will start at the border of
that group's bounding box. The \code{from} node must be inside that group.}

\item{bidirectional}{Logical. If \code{TRUE}, the arrow is drawn with
arrowheads in both directions. Use for symmetric relationships
(e.g., identical concepts via \verb{i 1}, mutual predictions). A single
strength value applies to both directions; if strengths differ by
direction, use two separate edges instead.}
}
\value{
A list of class \code{vast_edge}.
}
\description{
The generic edge constructor. All typed relationship helpers
(\code{\link[=vast_naming]{vast_naming()}}, \code{\link[=vast_causation]{vast_causation()}}, etc.) call this internally.
Use this directly when you need a custom relationship type or
a plain structural link with no type letter.
}
\examples{
vast_relation("X", "Y", type = "c", strength = 0.5)
vast_relation("A", "B", type = "", color = "gray60")

# Arrow pointing to the border of group "bio" instead of node "genes":
vast_causation("ext", "genes", lhead = "bio")

# Bidirectional implication (identity):
vast_implication("X", "Y", strength = 1, bidirectional = TRUE)

}
